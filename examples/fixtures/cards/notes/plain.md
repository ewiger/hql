# Plain Note

A CommonMark document with no metadata block and no wikilinks. It exists so the
corpus has a document whose `header.format` is `md`: its header is empty of
authored entries but still carries the derived name, path and format, and it has
no uplinks to traverse because plain Markdown has no link dialect.

It mentions Alice in prose without referring to her card.
