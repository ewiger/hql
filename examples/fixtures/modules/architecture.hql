// Imported claim fixture; historical structured-HQL syntax alternative.
// This represents an explicit assertion, not a semantic interpretation of a sentence.
declare relatedTo([[alice]], [[bob]]) {
    kind = daughter,
    confidence = 0.9,
    source = [[family-record]]
}
