// status: design-question
// feature: graph as presenter
// implementation: pending
// environment: knowledge-v1
// expected-type: Presentation
// expected: node-and-relation presentation over fixture evidence
// note: Reads the same source as the projection case, which types it as Graph. One spelling cannot mean both a Knowledge-to-Graph projection and a terminal presenter; the corpus keeps both readings until the naming is decided.
// alternatives: knowledge/graph.hql, presentation/graph-view.hql

knowledge
| graph
